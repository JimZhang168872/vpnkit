package profiles

// TODO(v1-phase8): Tests for the old profiles tab referencing internal/profiles
// package are removed because internal/profiles was deleted in Phase 6.
// Phase 8 TUI restructure will replace this tab with Groups/Sources and add
// new tests against those.
